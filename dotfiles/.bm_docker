alias gitp='gitt commit -a "update" && gitt push'
alias gitt='/usr/bin/git --git-dir=$HOME/docker/.git/ --work-tree=$HOME/docker'

function gita() {
        gitt add $@
        gitt commit -m "Added $@"
}
